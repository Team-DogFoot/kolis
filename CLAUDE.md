# kolis — 클로드코드 프로젝트 지침

이 저장소는 국립중앙도서관 "웹툰 납본·수집 대행 사업"의 KOLIS 자동화 도구다. 세션을 시작하면 **먼저 `docs/HANDOFF.md` 를 읽고, 이어서 `docs/FIELD-CHECKLIST.md` 를 읽는다.** 두 문서가 배경·합의 사항·현재 상태·다음 할 일을 담고 있다.

**유저가 "시작"이라고 하면 `docs/HANDOFF.md` 0절의 지시를 그대로 수행한다.** 유저는 다른 PC 에서 이 저장소를 받았을 뿐이고 이전 대화를 붙여 넣을 수 없다. 필요한 지시는 전부 문서 안에 있다.

## 지켜야 할 것 (2026-09-12 유저 확정)
- human-in-the-loop. 사람 판정 지점: 복본 판정, 반입 전 반입용 엑셀 확인, 저자전거·주제명 연결 승인, 점검 결과 확정.
- **KOLIS 에 요청을 보내는 일은 직원 입회·요청·동의 아래에서만.** 테스트·검증 명목의 요청 금지(콘텐츠ID 시험 조회 포함). 검증은 HAR·저장된 응답·`--preview` 로만. 첫 실제 실행은 실제 작품으로 직원과 함께.
- MODStoXL.exe·정리 매크로·다크네이머·하이웨어 SFTP 는 쓰지 않는다(파이썬으로 대체).
- LLM 보완은 `claude -p` 헤드리스가 기본. LLM 은 로컬 파일과 외부 플랫폼 페이지만 본다.
- 계정·비밀번호는 `.env`(git 제외)와 환경변수로만. 문서·코드·커밋·메모리에 절대 적지 않는다. 참고자료 PDF 의 평문 계정을 옮기지 않는다.
- 도서관 제공 원본(가이드·매뉴얼·지침서·양식·캡처)은 `docs/source/` 에 있다(2026-09-18 유저 지시로 포함). 막히면 그걸 본다. 샘플 원고 zip·HAR·계정이 든 파일은 넣지 않는다.
- 유저가 정한 범위를 비용·시간 이유로 임의로 줄이지 않는다. 질문에는 답만 하고, 행동은 지시가 있을 때.

## 메모리
`docs/memory/` 는 이 프로젝트의 클로드코드 영속 메모리 사본이다. 세션을 시작할 때 **실제 세션 메모리 폴더**에 복사하면 이전 세션의 기억이 이어진다.
이 도서관 PC 는 세션이 상위 폴더에서 열려 `%USERPROFILE%\.claude\projects\C--Users-User-dataclip\memory\` 이다(저장소 경로 기준이 아님). `MEMORY.md` 는 색인만 병합한다.
세션 중 메모리를 고쳤으면 `docs/memory/` 에도 복사해 커밋한다(세션 종료 절차).

## 프로그램
`bin\app.vbs`(바탕화면 바로 가기)로 창을 띄운다. 코드를 고친 뒤에는 `pythonw` 를 종료하고 다시 띄워야 반영된다. KOLIS 조작 단계(5·6·7)는 직원 입회 아래에서만, 클릭 후 반드시 상태를 확인한다.

## 보고 규칙
`~/works/dog-foot/CLAUDE.md` 의 브리핑 규칙을 따른다(요약·누락 금지, 용어 풀이, 비유 금지, 결론은 유저 의도 원문과 대응).
